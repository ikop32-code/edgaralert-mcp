# Copy to .env (or export these in your shell) for local development with
# `npm run dev`. Never commit a real API key.

EDGARALERT_API_KEY=EA_replace_with_your_enterprise_key

# Optional. Only set this if you're testing against staging/local.
# EDGARALERT_BASE_URL=https://api.edgaralert.com/api/v1
